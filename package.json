{
  "version": "1.0.0",
  "name": "jira-info",
  "main": "lib/index.js",
  "scripts": {
    "start": "babel-node src/index.js",
    "test": "mocha --compilers js:babel-register",
    "lint": "eslint .",
    "build": "babel src -d lib",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-register": "^6.9.0",
    "chai": "^3.5.0",
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "colors": "^1.1.2",
    "node-fetch": "^1.5.3",
    "prompt": "^1.0.0"
  },
  "license": "ISC",
  "directories": {
    "test": "test"
  },
  "preferGlobal": "true",
  "bin": {
    "jira-info": "lib/index.js"
  },
  "description": "Use `.jira-inforc` in the current working directory to store configs you don't want to keep repeating.  You can store your password, but that's probably not a good idea.  You will be prompted for anything missing from the file as needed.",
  "author": ""
}
